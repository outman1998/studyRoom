import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.studyroom',
  appName: 'studyroom',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
